---
name: <kebab-name-matching-directory>
description: >-
  <What it does, in one clause.> Use when <the contexts and phrasings that
  should route here, including ones that never name the domain>. Not for
  <the near-miss that belongs elsewhere> (use <neighbour> instead).
---

# <Title>

<One or two sentences: the job, and the one thing the agent would get wrong
without this skill.>

## Steps

1. <First action — a command or a file to read, not a principle.>
2. <Next action.>
3. <Validation: run `scripts/<check>.py`, fix, repeat until it passes.>

## Output

```text
<The exact shape expected back: a file, a diff, a report section.>
```

## Gotchas

- <An environment fact that defies a reasonable assumption.>
- <A correction the user has made that must not recur.>

<!-- Only when needed:
Read `references/<file>.md` when <the specific condition>.
-->
