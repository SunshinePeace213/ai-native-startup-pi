# Clean theme for pytest-sugar: color only carries meaning (green pass, red fail).
# Keys/values: Theme fields in pytest_sugar.py; termcolor names or "none".
[theme]
header = none
path = none
skipped = yellow
progressbar_background = none

[sugar]
progressbar_length = 10
